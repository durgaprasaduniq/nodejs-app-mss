apiVersion: v1
kind: Service
metadata:
  name: nodejs-service
  labels:
    env: app
    value: new
spec:
  type: LoadBalancer
  selector:
    env: app
    value: new
  ports:
    - port: 9981
      targetPort: 9981

